# SaaS Starter

A reusable Bun + Turborepo monorepo starter for building SaaS applications.

## Quickstart

```bash
bun install
docker compose up -d
bun run db:migrate
bun run db:seed
bun run dev
```
