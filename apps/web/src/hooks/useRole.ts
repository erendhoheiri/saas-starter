// Role will be populated when we have org membership data in Task 4.6.
// For now this is a typed stub so consumers can import and call it.
export function useRole(): string | null {
  return null
}
